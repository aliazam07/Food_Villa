 import { sum } from "../sum";
 
// This is how we write test cases in JavaScript
 test("Check sum of 2 positive numbers", ()=>{
    expect(sum(2,5)).toBe(7);
})

/* 
* install react testing library
*install jest
* configure jest.config
*create __tests__ folder under components
* create test file 
* now for import to be compatible with code intstall jest with parcel
* now configure babel.config.js



*/